[package]
name = "neobrood"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.9"
iyes_loopless = "0.9"

# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3
